---
export interface Props {
  title: string;
  message: string;
  email: string;
  phone: string;
}

const { title, message, email, phone } = Astro.props;
---

<section class="bg-background-light dark:bg-background-dark text-text py-24 px-6 md:px-12 lg:px-24 scroll-mt-20 text-center">
  <h2 class="text-4xl font-heading font-bold text-primary mb-4">{title}</h2>
  <p class="text-lg text-text-secondary-light dark:text-text-secondary-dark mb-8">{message}</p>
  <div class="text-lg space-y-2">
    <p>📧 <a href={`mailto:${email}`} class="text-primary underline">{email}</a></p>
    <p>📞 <span class="font-semibold">{phone}</span></p>
  </div>
</section>
